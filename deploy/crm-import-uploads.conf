# systemd-tmpfiles safety net: /etc/tmpfiles.d/crm-import-uploads.conf
#
# The app expires sessions itself, but this guarantees uploaded lead tables
# cannot survive on disk if the process is killed mid-session.
d /var/lib/crm-import/uploads 0700 crmimport crmimport 3h
